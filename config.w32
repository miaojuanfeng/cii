// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cii", "for cii support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cii", "enable cii support", "no");

if (PHP_CII != "no") {
	EXTENSION("cii", "cii.c");
}

